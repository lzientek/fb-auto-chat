<UserControl x:Class="FbSpammer.UserControls.FbUserSelectUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:FbSpammer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <UserControl.Resources>
        <viewModels:UserSelectViewModel x:Key="Model" />

    </UserControl.Resources>
    <Grid>
        <ComboBox IsEditable="True" 
                  ItemsSource="{Binding Source={StaticResource Model},Path=FbSmallUsers}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding Path=thumbSrc,Converter={StaticResource ToImageConverter}}" />
                        <TextBlock Grid.Column="1" Text="{Binding Path=name}" />
                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
